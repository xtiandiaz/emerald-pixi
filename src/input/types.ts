export enum PointerEventKey {
  Down = 'pointerdown',
  Move = 'pointermove',
  Up = 'pointerup',
  UpOutside = 'pointerupoutside',
}

export enum GestureKey {
  Tap = 'TAP',
  Drag = 'DRAG',
  Swipe = 'SWIPE',
}

export enum GesturePhase {
  Began = 'BEGAN',
  Changed = 'CHANGED',
  Ended = 'ENDED',
}
